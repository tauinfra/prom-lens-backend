apiVersion: v1
kind: Service
metadata:
  name: prom-lens-backend
  namespace: prom-lens
  labels:
    app.kubernetes.io/name: prom-lens-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prom-lens-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
